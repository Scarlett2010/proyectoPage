---
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_DESCRIPTION, SITE_TITLE } from "../consts";
import { getCollection, type CollectionEntry } from "astro:content";

// Cargamos todos los servicios
let servicios: CollectionEntry<"services">[] = await getCollection("services");

// Orden deseado
const orden = {
  "Derecho Penal": 1,
  "Derecho Constitucional": 2,
  "Derecho Civil": 3,
  "Derecho de Familia": 3, // Igual que Derecho Civil para agrupar juntos
};

// Reordenamos según el orden definido
servicios = servicios.sort(
  (a, b) => (orden[a.data.title] ?? 99) - (orden[b.data.title] ?? 99)
);

// 🔹 Rutas personalizadas por servicio
const rutas = {
  "Derecho Penal": "/derechop",
  "Derecho Constitucional": "/derechoc",
  "Derecho Civil": "/derechocivil",
  "Derecho de Familia": "/derechof",
};
---

<html lang="es">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main
      style="max-width: 900px; margin: auto; padding: 2rem;"
      class="principal"
    >
      <h5>Representación legal experta | Justicia y resultados garantizados</h5>
      <p>
        Cada caso es único, cada cliente es prioritario. Ofrezco acompañamiento
        jurídico integral desde la consulta inicial hasta la resolución
        definitiva, defendiendo sus derechos con experiencia y dedicación
        absoluta.
      </p>
      <img
        class="perfil slide-in-left"
        src="/src/assets/dr.jo.jpg"
        alt="Perfil de abogado"
        style="width: 320px; display: block; margin: 2rem auto;"
      />
      <a
        href="/form"
        style="
        padding: 0.6em 1.2em; 
        font-size: 1rem; 
        border-radius: 4px; 
        border:none; 
        background: linear-gradient(135deg, #d97706, #f59e0b);
        color: white; 
        cursor: pointer;
        "
      >
        Trabajemos juntos
      </a>

      <main class="services">
        <h4 style="margin-top: 0">Servicios tratados como una especialidad</h4>
        <section class="cards-container">
          {
            servicios.map((servicio) => (
              <a
                href={rutas[servicio.data.title] || "#"}
                class="card"
                style="
                  text-decoration: none;
                  border: 1px solid #ccc;
                  padding: 1rem;
                  border-radius: 8px;
                  color: #000;
                  transition: box-shadow 0.3s ease;
                  display: block;
                "
                key={servicio.id}
              >
                <img
                  src={
                    servicio.data.image ||
                    (servicio.data.title === "Derecho Civil"
                      ? "/src/assets/derechocivil2.jpg"
                      : servicio.data.title === "Derecho Constitucional"
                        ? "/src/assets/derechocconstitucional2.webp"
                        : servicio.data.title === "Derecho Penal"
                          ? "/src/assets/derechopenal.webp"
                          : servicio.data.title === "Derecho de Familia"
                            ? "/src/assets/derechof2.jpg"
                            : "")
                  }
                  alt={servicio.data.title}
                  class="card-image"
                />
                <h5 style="margin-top: 0;">{servicio.data.title}</h5>
                <p>{servicio.data.description}</p>
              </a>
            ))
          }
        </section>
      </main>

      <main class="second-container">
        <h3 class="scroll-animate" data-animation="slide-up">
          ¿Necesitas asesoría legal?
        </h3>
        <img
          class="isotipo scroll-animate"
          data-animation="slide-in-left"
          src="/src/assets/lexloci2.jpg"
          alt="Perfil de abogado"
          style="width: 320px; display: block; margin: 2rem auto;"
        />
        <p class="scroll-animate" data-animation="slide-up">
          Justicia con visión estratégica y compromiso ético. Contáctame y
          descubre cómo puedo ayudarte a proteger tus derechos y alcanzar los
          mejores resultados en tu caso.
        </p>
        <a
          href="/form"
          class="scroll-animate"
          data-animation="slide-up"
          style="
            padding: 0.6em 1.2em; 
            font-size: 1rem; 
            border-radius: 4px; 
            border:none; 
            background: linear-gradient(135deg, #004494, #0057b8, #1e73e6);
            color: white; 
            cursor: pointer;
          "
        >
          Trabajemos juntos
        </a>
      </main>
    </main>
    <Footer />

    <script>
      // Intersection Observer para animaciones en scroll
      const observerOptions = {
        threshold: 0.1,
        rootMargin: "0px 0px -50px 0px",
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            const element = entry.target;
            const animation = element.getAttribute("data-animation");

            if (animation) {
              element.classList.add(animation);
              // Una vez que se anima, no necesitamos seguir observando
              observer.unobserve(element);
            }
          }
        });
      }, observerOptions);

      // Observar todos los elementos con la clase scroll-animate
      document.addEventListener("DOMContentLoaded", () => {
        const elementsToAnimate = document.querySelectorAll(".scroll-animate");
        elementsToAnimate.forEach((el) => {
          // Inicialmente ocultos
          el.style.opacity = "0";
          observer.observe(el);
        });
      });
    </script>
  </body>
</html>
<style>
  html,
  body {
    background: rgb(233, 236, 249);
    margin: 0;
    min-height: 100%;
    width: 100%;
    max-width: 100%;
    overflow-x: hidden;
  }

  /* Animaciones de traslación */
  @keyframes slideInLeft {
    0% {
      transform: translateX(-100px);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes slideInRight {
    0% {
      transform: translateX(100px);
      opacity: 0;
    }
    100% {
      transform: translateX(0);
      opacity: 1;
    }
  }

  @keyframes slideUp {
    0% {
      transform: translateY(50px);
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes slideDown {
    0% {
      transform: translateY(-50px);
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 1;
    }
  }

  /* Clases para aplicar las animaciones */
  .slide-in-left {
    animation: slideInLeft 1s ease-out forwards;
  }

  .slide-in-right {
    animation: slideInRight 1s ease-out forwards;
  }

  .slide-up {
    animation: slideUp 1s ease-out forwards;
  }

  .slide-down {
    animation: slideDown 1s ease-out forwards;
  }

  /* Elementos con scroll-animate empiezan invisibles */
  .scroll-animate {
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  /* Retrasos escalonados para elementos consecutivos */
  .scroll-animate:nth-child(2) {
    animation-delay: 0.2s;
  }

  .scroll-animate:nth-child(3) {
    animation-delay: 0.4s;
  }

  .scroll-animate:nth-child(4) {
    animation-delay: 0.6s;
  }

  .principal {
    margin: 0;
    padding: 1rem;
    text-align: center;
    width: 100%;
    font-family: Arial, sans-serif;
    box-sizing: border-box;
  }

  .principal h5 {
    text-align: center;
    color: #0041c4;
  }

  .principal p {
    text-align: center;
    font-size: 1.1rem;
    line-height: 1.6;
  }

  .services {
    margin-top: 2rem;
    text-align: center;
    width: 100%;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
    padding: 2rem 1rem;
    background-color: #ffffff;
    box-shadow: 0 4px 8px #90bce6;
    border: 1px solid #5383ea;
    border-radius: 15px;
    box-sizing: border-box;
  }

  .services h4 {
    text-align: center;
    margin-bottom: 1rem;
    color: #0041c4;
    font-size: 2rem;
    font-weight: bold;
  }

  .cards-container {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    margin-top: 1.5rem;
    width: 100%;
    box-sizing: border-box;
  }

  .cards-container h5 {
    text-align: center;
    color: #d97706;
  }

  .cards-container p {
    text-align: justify;
  }

  .card {
    border-radius: 15px;
    box-shadow: 0 4px 8px #94c6f5;
    border: 2px solid #5383ea;
    display: flex;
    flex-direction: column;
    transition:
      box-shadow 0.3s ease,
      transform 0.25s ease;
    background: #fff;
    padding: 1.5rem 1rem;
    box-sizing: border-box;
  }

  .card:hover {
    box-shadow: 0 8px 15px rgb(0 0 0 / 0.2);
    transform: translateY(-4px);
  }

  .card h5 {
    margin: 0 0 0.7rem 0;
    font-weight: 700;
    font-size: 1.3rem;
  }

  .card p {
    margin: 0;
    font-size: 1rem;
    opacity: 0.8;
  }

  .card-image {
    width: 180px;
    max-width: 100%;
    height: auto;
    object-fit: contain;
    border-radius: 15%;
    margin-bottom: 1rem;
    border: 3px solid #686869;
    display: block;
    margin-left: auto;
    margin-right: auto;
  }

  .perfil {
    border: #7aa8f7 4px solid;
    max-width: 420px;
    width: 100%;
    height: auto;
    display: block;
    margin: 2rem auto;
  }

  .card-icon {
    align-self: flex-end;
    width: 30px;
    height: 30px;
    background-color: #547ccd;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    margin-bottom: 0.8rem;
    color: white;
    font-weight: bold;
    font-size: 1.2rem;
  }

  .second-container {
    width: 100%;
    margin: 0 auto;
    text-align: center;
    box-sizing: border-box;
    max-width: 600px;
    padding: 2rem 1rem;
  }

  .second-container h3 {
    text-align: center;
    color: #0041c4;
  }

  .second-container p {
    font-size: 1.2rem;
    margin-bottom: 1rem;
    text-align: center;
  }

  .isotipo {
    max-width: 320px;
    width: 100%;
    height: auto;
    display: block;
    margin: 2rem auto;
  }

  @media (max-width: 600px) {
    .services,
    .second-container {
      max-width: 98vw;
      padding: 1rem 0.5rem;
    }
    .cards-container {
      grid-template-columns: 1fr;
      gap: 1rem;
    }
    .card-image {
      width: 80px;
      max-width: 90vw;
    }
    .perfil,
    .isotipo {
      max-width: 260px;
    }
  }

  @media (max-width: 380px) {
    body {
      margin: 0;
      padding: 0;
      overflow-x: hidden;
      word-break: break-word;
    }

    .principal,
    .services,
    .second-container,
    .cards-container,
    .card {
      width: 100%;
      margin: 0;
      padding: 0.5rem;
      box-sizing: border-box;
    }

    .card {
      flex: 1 1 100%;
    }

    .card-image {
      width: 60px;
      max-width: 90vw;
    }

    img,
    .perfil,
    .isotipo {
      width: 100%;
      max-width: 200px;
      height: auto;
      margin: 0 auto;
      display: block;
    }

    a {
      font-size: 0.85rem;
      padding: 0.4em 0.8em;
      margin: 0.5rem auto;
      display: inline-block;
    }

    footer,
    .Footer {
      margin: 0;
      padding: 0.5rem;
      width: 100%;
    }
    p,
    h5,
    h4,
    h3 {
      word-break: break-word;
      hyphens: auto;
    }
  }
</style>
